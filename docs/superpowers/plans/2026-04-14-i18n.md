# 国际化 (i18n) 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为 Tooltip、Loot Tracker、设置面板、升级预估添加中英双语支持

**Architecture:** 在 utils.js 添加 t() 翻译函数，创建 i18n.js 存放翻译文本，各文件使用 t() 函数进行国际化

**Tech Stack:** JavaScript, Tampermonkey Userscript

---

## 文件变更

| 文件 | 变更 | 职责 |
|------|------|------|
| `src/i18n.js` | 新建 | 存放所有翻译文本映射 |
| `src/utils.js` | 修改 | 添加 `t()` 翻译函数 |
| `src/tooltipManager.js` | 修改 | Tooltip 表头和数值标签国际化 |
| `src/LostTrackerExpectEstimate.js` | 修改 | Loot Tracker 文本国际化 |
| `src/settingsManager.js` | 修改 | 设置面板文本国际化 |

---

### Task 1: 创建 src/i18n.js 翻译文本文件

**Files:**
- Create: `src/i18n.js`

- [ ] **Step 1: 创建 i18n.js 文件**

创建 `src/i18n.js`，内容如下：

```javascript
// 国际化翻译文本
export const i18n = {
    tooltip: {
        material: { zh: '原料', en: 'Material' },
        qty: { zh: '数量', en: 'Qty' },
        ask: { zh: '出售价', en: 'Ask' },
        bid: { zh: '收购价', en: 'Bid' },
        qtyPerHour: { zh: '数量/小时', en: 'Qty/h' },
        output: { zh: '产出', en: 'Output' },
        hourlyExpenditure: { zh: '每小时支出', en: 'Hourly Expenditure' },
        hourlyIncome: { zh: '每小时收入', en: 'Hourly Income' },
        actionsPerHour: { zh: '每小时动作', en: 'Actions/h' },
        teaReduction: { zh: '茶减少消耗', en: 'Tea Reduction' },
        expPerAction: { zh: '单次经验值', en: 'Exp/Action' },
        expPerHour: { zh: '每小时经验值', en: 'Exp/h' },
        hourlyProfit: { zh: '每小时利润', en: 'Hourly Profit' },
        levelUpEst: { zh: '升级预估', en: 'Level Up Est.' },
        total: { zh: '合计', en: 'Total' },
    },
    lootTracker: {
        expense: { zh: '支出', en: 'Expense' },
        revenue: { zh: '收入', en: 'Revenue' },
        expectedProfit: { zh: '预期盈利', en: 'Expected Profit' },
        actualProfit: { zh: '实现盈利', en: 'Actual Profit' },
        duration: { zh: '统计时长', en: 'Duration' },
        netProfit: { zh: '净利润', en: 'Net Profit' },
        vsExpected: { zh: '较预期', en: 'vs Expected' },
    },
    settings: {
        profitSettings: { zh: '收益设置', en: 'Profit Settings' },
        materialPriceMode: { zh: '原料进货方式', en: 'Material Price Mode' },
        highAsk: { zh: '高买', en: 'High Ask' },
        lowBid: { zh: '低买', en: 'Low Bid' },
        productPriceMode: { zh: '产品出货方式', en: 'Product Price Mode' },
        highAskSell: { zh: '高卖', en: 'High Ask' },
        lowBidSell: { zh: '低卖', en: 'Low Bid' },
        actionCategories: { zh: '显示的动作分类', en: 'Action Categories' },
        dataSource: { zh: '数据来源', en: 'Data Source' },
        levelUpDisplayCount: { zh: '升级预估显示级数', en: 'Level Up Display Count' },
        save: { zh: '保存', en: 'Save' },
        cancel: { zh: '取消', en: 'Cancel' },
    }
};
```

- [ ] **Step 2: 提交**

```bash
git add src/i18n.js
git commit -m "feat: add i18n translation file"
```

---

### Task 2: 在 utils.js 添加 t() 翻译函数

**Files:**
- Modify: `src/utils.js`

- [ ] **Step 1: 在文件顶部导入 i18n**

在 `src/utils.js` 顶部添加：

```javascript
import { i18n } from './i18n.js';
```

- [ ] **Step 2: 在文件末尾添加 t() 函数**

在 `TimeSpan` 对象定义之后添加：

```javascript
/**
 * 翻译函数
 * @param {string} zh - 中文文本
 * @param {string} en - 英文文本
 * @returns {string} - 根据游戏设置返回对应语言文本
 */
export function t(zh, en) {
    return globals.isZHInGameSetting ? zh : en;
}
```

- [ ] **Step 3: 提交**

```bash
git add src/utils.js
git commit -m "feat: add t() translation function to utils"
```

---

### Task 3: 国际化 tooltipManager.js

**Files:**
- Modify: `src/tooltipManager.js`

- [ ] **Step 1: 添加导入**

在文件顶部添加：

```javascript
import { t } from './utils.js';
```

- [ ] **Step 2: 替换 Tooltip 表头文本**

替换以下文本：
- `原料` → `t('原料', 'Material')`
- `数量` → `t('数量', 'Qty')`
- `出售价` → `t('出售价', 'Ask')`
- `收购价` → `t('收购价', 'Bid')`
- `数量/小时` → `t('数量/小时', 'Qty/h')`
- `产出` → `t('产出', 'Output')`
- `合计` → `t('合计', 'Total')`

替换数值标签：
- `每小时支出:` → `${t('每小时支出', 'Hourly Expenditure')}:`
- `每小时收入` → `t('每小时收入', 'Hourly Income')`
- `每小时动作:` → `${t('每小时动作', 'Actions/h')}:`
- `茶减少消耗:` → `${t('茶减少消耗', 'Tea Reduction')}:`
- `单次经验值:` → `${t('单次经验值', 'Exp/Action')}:`
- `每小时经验值:` → `${t('每小时经验值', 'Exp/h')}:`
- `每小时利润:` → `${t('每小时利润', 'Hourly Profit')}:`

替换 Buff 类型名称：
- `社区` → `t('社区', 'Community')`
- `茶` → `t('茶', 'Tea')`
- `装备` → `t('装备', 'Equipment')`
- `等级` → `t('等级', 'Level')`
- `房子` → `t('房子', 'House')`
- `成就` → `t('成就', 'Achievement')`
- `卷轴` → `t('卷轴', 'Scroll')`
- `MooPass` → `t('MooPass', 'MooPass')`

替换升级预估文本：
- `升级预估` → `t('升级预估', 'Level Up Est.')`
- `级` → `t('级', '')`

- [ ] **Step 3: 提交**

```bash
git add src/tooltipManager.js
git commit -m "feat: i18n tooltip text in tooltipManager"
```

---

### Task 4: 国际化 LostTrackerExpectEstimate.js

**Files:**
- Modify: `src/LostTrackerExpectEstimate.js`

- [ ] **Step 1: 添加导入**

在文件顶部添加：

```javascript
import { t } from './utils.js';
```

- [ ] **Step 2: 替换 Loot Tracker 文本**

替换以下文本：
- `支出:` → `${t('支出', 'Expense')}:`
- `收入:` → `${t('收入', 'Revenue')}:`
- `预期盈利：` → `${t('预期盈利', 'Expected Profit')}：`
- `实现盈利:` → `${t('实现盈利', 'Actual Profit')}:`
- `统计时长：` → `${t('统计时长', 'Duration')}：`
- `净利润:` → `${t('净利润', 'Net Profit')}:`
- `较预期:` → `${t('较预期', 'vs Expected')}:`
- `天` → `t('天', 'd')`

- [ ] **Step 3: 提交**

```bash
git add src/LostTrackerExpectEstimate.js
git commit -m "feat: i18n text in LostTrackerExpectEstimate"
```

---

### Task 5: 国际化 settingsManager.js

**Files:**
- Modify: `src/settingsManager.js`

- [ ] **Step 1: 添加导入**

在文件顶部添加：

```javascript
import { t } from './utils.js';
```

- [ ] **Step 2: 替换设置面板文本**

替换以下文本：
- `收益设置` → `t('收益设置', 'Profit Settings')`
- `原料进货方式` → `t('原料进货方式', 'Material Price Mode')`
- `高买` → `t('高买', 'High Ask')`
- `低买` → `t('低买', 'Low Bid')`
- `产品出货方式` → `t('产品出货方式', 'Product Price Mode')`
- `高卖` → `t('高卖', 'High Ask')`
- `低卖` → `t('低卖', 'Low Bid')`
- `显示的动作分类` → `t('显示的动作分类', 'Action Categories')`
- `数据来源` → `t('数据来源', 'Data Source')`
- `升级预估显示级数` → `t('升级预估显示级数', 'Level Up Display Count')`
- `保存` → `t('保存', 'Save')`
- `取消` → `t('取消', 'Cancel')`
- `级` → `t('级', 'lv')`

- [ ] **Step 3: 提交**

```bash
git add src/settingsManager.js
git commit -m "feat: i18n text in settingsManager"
```

---

### Task 6: 构建并测试

**Files:**
- Build: `npm run build`

- [ ] **Step 1: 构建项目**

```bash
npm run build
```

Expected: 构建成功，无错误

- [ ] **Step 2: 验证生成的代码**

```bash
grep -n "i18n\|t('" dist/MWI-Profit-Panel.user.js | head -30
```

Expected: 显示包含 i18n 和 t() 函数的代码

- [ ] **Step 3: 提交**

```bash
git add -A
git commit -m "chore: build with i18n support"
```

---

## Spec Coverage Check

| 设计需求 | 实现任务 |
|----------|----------|
| i18n.js 翻译文件 | Task 1 |
| t() 翻译函数 | Task 2 |
| Tooltip 国际化 | Task 3 |
| Loot Tracker 国际化 | Task 4 |
| 设置面板国际化 | Task 5 |
| 构建验证 | Task 6 |

## Rollback Plan

如需回滚：
```bash
git reset --hard HEAD~6
```
